<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background: transparent;
      font-family: 'Inter', sans-serif;
    }

    .login-card {
      width: 100%;
      max-width: 380px;
      padding: 2rem;
      border-radius: 1rem;
      border: 1px solid rgba(255, 255, 255, 0.1);
      background: rgba(255, 255, 255, 0.03);
      backdrop-filter: blur(20px);
      box-shadow: 0 0 20px rgba(255, 255, 255, 0.05);
    }

    .input-field {
      background: transparent;
      border: 1px solid rgba(255, 255, 255, 0.1);
      padding: 0.75rem 1rem;
      border-radius: 0.5rem;
      color: white;
      font-size: 0.9rem;
    }

    .input-field::placeholder {
      color: rgba(255, 255, 255, 0.4);
    }

    .submit-btn {
      background: #00c3ff;
      color: white;
      font-weight: 500;
      padding: 0.75rem;
      border-radius: 0.5rem;
      transition: all 0.3s ease;
    }

    .submit-btn:hover {
      background: #00a4d6;
      transform: scale(1.03);
    }

    .fade-in {
      animation: fadeIn 0.6s ease forwards;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="flex items-center justify-center min-h-screen text-white">

  <div class="login-card fade-in">
    <h2 class="text-xl font-semibold text-center mb-6">GrowID Login</h2>

    <form 
      method="POST" 
      action="/player/growid/login/validate" 
      id="loginForm"
      accept-charset="UTF-8" 
      autocomplete="off">

      <input type="hidden" name="type" value="log">
      <input type="hidden" name="_token" value="<%= JSON.stringify(data) %>"/>

      <div class="mb-4">
        <input 
          type="text" 
          name="growId" 
          id="growId" 
          placeholder="GrowID" 
          class="w-full input-field"/>
      </div>

      <div class="mb-4 relative">
        <input 
          type="password" 
          name="password" 
          id="password" 
          placeholder="Password" 
          class="w-full input-field"/>
        <button type="button" onclick="togglePassword()" class="absolute right-4 top-3 text-sm text-white/60">
          <i id="eyeIcon" class="fas fa-eye"></i>
        </button>
      </div>

      <button type="submit" class="w-full submit-btn">Login</button>
    </form>
  </div>

  <script>
    function togglePassword() {
      const pwd = document.getElementById('password');
      const icon = document.getElementById('eyeIcon');
      if (pwd.type === 'password') {
        pwd.type = 'text';
        icon.classList.replace('fa-eye', 'fa-eye-slash');
      } else {
        pwd.type = 'password';
        icon.classList.replace('fa-eye-slash', 'fa-eye');
      }
    }

    document.getElementById('loginForm').addEventListener('submit', function(event) {
      const growId = document.getElementById('growId').value;
      const password = document.getElementById('password').value;

      if (!growId && !password) {
        // Handle the case where both fields are empty
        event.preventDefault();
        alert('Please enter your GrowID and password.');
      }
    });
  </script>
</body>
</html>
