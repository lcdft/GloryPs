<!DOCTYPE html>
<html lang="en" style="background-color: transparent; width: 100%; height: 100%;">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>Growtopia - Login</title>
    <link rel="icon" type="image/png" href="https://s3.eu-west-1.amazonaws.com/cdn.growtopiagame.com/website/resources/assets/images/growtopia.ico" sizes="16x16">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
    <div class="w-full h-full flex items-center justify-center">
        <div class="w-[360px] bg-white/5 backdrop-blur-md rounded-lg p-6 shadow-lg">
            <h1 class="text-2xl font-semibold mb-4 text-center">Growtopia - Login</h1>

            <form id="auth-form" action="/player/growid/login/validate" method="post" class="space-y-3">
                <input type="hidden" name="type" value="login">
                <input type="hidden" name="_token" value="">

                <div>
                    <label class="text-sm">GrowID</label>
                    <input name="growId" id="growId" class="w-full p-2 rounded mt-1 bg-white/10" placeholder="Your growID">
                </div>
                <div>
                    <label class="text-sm">Password</label>
                    <input type="password" name="password" id="password" class="w-full p-2 rounded mt-1 bg-white/10" placeholder="Password">
                </div>

                <div class="flex items-center justify-between">
                    <label class="flex items-center gap-2 text-sm"><input type="checkbox" id="remember" checked> Remember account</label>
                    <a href="/player/login/register" id="go-register" class="text-sm text-blue-400">Register</a>
                </div>

                <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded">Login</button>
            </form>
        </div>
    </div>

    <!-- Accounts chooser modal (opens on page load) -->
    <div id="accounts-chooser" class="fixed inset-0 flex items-center justify-center bg-black/60 hidden">
        <div class="bg-white/5 backdrop-blur-md rounded-lg p-4 w-[380px]">
            <div class="flex justify-between items-center mb-3">
                <h2 class="text-lg font-semibold">Choose an account</h2>
                <button id="close-chooser" class="text-sm text-gray-300">Close</button>
            </div>
            <div id="accounts-list" class="space-y-2 max-h-48 overflow-auto"></div>
            <div class="mt-4 flex gap-2">
                <button id="use-manual" class="flex-1 bg-transparent border border-gray-300 py-2 rounded">Use manual</button>
                <button id="clear-all" class="flex-1 bg-red-600 py-2 rounded">Clear All</button>
            </div>
        </div>
    </div>

    <script src="/js/accounts.js"></script>
</body>
</html>
